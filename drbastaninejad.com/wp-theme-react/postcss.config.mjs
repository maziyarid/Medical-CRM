// PostCSS config — intentionally empty.
// Tailwind v4 is handled entirely by @tailwindcss/vite (see vite.config.ts).
// Do NOT add tailwindcss or autoprefixer here; it will conflict with the Vite plugin.
export default { plugins: {} };
